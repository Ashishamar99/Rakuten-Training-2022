package com.groovy.demo.optionals

class OptionalsDemo {
	static void main(String[] args) {
		def x_int = 10
		def x = 10
		if(x.TYPE == x_int.TYPE) {
			println("We can see that we don't have to specify the datatype with def.")	
		}
	}
}
